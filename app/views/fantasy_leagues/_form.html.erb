<%= form_with(model: fantasy_league) do |form| %>
  <% if fantasy_league.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fantasy_league.errors.count, "error") %> prohibited this fantasy_league from being saved:</h2>

      <ul>
        <% fantasy_league.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="league_name">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <div class="league_description">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
